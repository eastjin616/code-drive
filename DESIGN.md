# Design System — @eastjin616/code-drive

> Auto-generated by **code-drive design**. Design tokens extracted from source code.

## No design tokens found

CDD scanned the project but did not find CSS custom properties, Tailwind theme values, or TypeScript theme constants that look like design tokens.

## AI Usage Guidance

- Treat this file as a signal that the project does not yet expose a reusable design system in code.
- Before UI work, inspect existing components and styles directly, then add reusable tokens when introducing repeated colors, spacing, typography, radius, or shadow values.
- Prefer CSS custom properties, Tailwind theme extensions, or exported theme constants so future `cdd design` runs can document them here.
- Run `cdd design` again after adding or changing design tokens.

## Token Sources CDD Detects

- CSS custom properties such as `--color-primary`, `--font-sans`, `--space-4`, `--radius-md`, and `--shadow-card`.
- TypeScript constants named `colors`, `theme`, `tokens`, `palette`, `styles`, or `designSystem`.
- Tailwind config files named `tailwind.config.js`, `tailwind.config.ts`, `tailwind.config.mjs`, or `tailwind.config.mts`.
